version: '3'
services:
  db:
    image: mongo:latest
    ports:
      - 27017:27017
    volumes:
      - ./data:/data/db
  backend:
    build: .
    expose: 
      - '8080'
    depends_on: 
      - db

# run with
# docker-compose up